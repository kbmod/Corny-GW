# Everything the app uses is reachable from Compose entry points; the defaults
# in proguard-android-optimize.txt are sufficient. Keep line numbers so that
# crash reports from release builds stay readable.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
